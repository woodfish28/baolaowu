# 老余数字人 · 全量综合包

> 保来屋保险经纪人余仁哲的对话分身。**不需要云端后端**，在对方自己的 WorkBuddy 里运行。
> 没配 IMA 也能用——可选的离线快照需自行从 IMA 导出。
>
> **这是「一个包通吃」的全量版**：WorkBuddy 技能（4 个）+ IMA 智能体提示词 + 共享知识库配置，全部打在一个 zip 里。接收方只需解压安装一次，就能同时拥有 WorkBuddy 端和 IMA 端两种用法。
>
> 版本 v1.0（综合版） · 打包日期 2026-09-14

---

## 接收方：三步装好

### 第 1 步 · 解压技能

把 `skills/` 下的四个目录，解压到你的 WorkBuddy 技能目录：

- **用户级**（推荐，所有项目都能用）：`~/.workbuddy/skills/`
  - Windows：`C:\Users\<你的用户名>\.workbuddy\skills\`
  - macOS / Linux：`~/.workbuddy/skills/`
- **项目级**（只在当前项目生效）：`<你的工作空间>/.workbuddy/skills/`

四个目录：

| 目录 | 作用 | 是否必需 |
|---|---|---|
| `laoyu-digital-human` | 主技能：检索路由、专项应答、合规边界 | ✅ 必需 |
| `yurenzhe-perspective` | 老余的思维框架与表达 DNA | ✅ 强烈建议（决定"老余味"） |
| `laoyu-client-grill` | 客户需求澄清三步法 | ⭕ 可选 |
| `baolaiwu` | 通用保险经纪工作台 | ⭕ 可选 |

### 第 2 步 · 验证离线检索（不配 IMA 也先做这步）

```bash
cd ~/.workbuddy/skills/laoyu-digital-human
python scripts/kb_search.py "护工卡"
```

能返回老余的文章，就说明装好了。脚本纯标准库，Python 3.7+，无需 pip install。

### 第 3 步 ·（可选）接 IMA，解锁产品库

在 WorkBuddy 的连接器里连接「ima知识库」，然后二选一：

- **点开链接加入共享知识库**（`config.json → kb_share_links`，如果作者填了的话）——推荐；
- 或者在你自己的 IMA 里建四个同名库，把 id 填进 `config.json → kb_priority`。

详见 `skills/laoyu-digital-human/references/offline-kb.md`。

---

## ⚠️ 关于「知识库能不能自动安装」

**不能，这个得先讲清楚。**

IMA 的知识库是**绑定 IMA 账号的云端资源**，不是一个能塞进 zip 的文件。它的 OpenAPI
只提供「检索 / 列出 / 导入内容」，**既没有创建知识库的接口，也没有把库授权给他人的接口**。

所以任何技能包都没法替你自动建好知识库——这是 IMA 的产品机制，不是打包方式的问题。

**能自动的只有三件事**：

1. 按名称找到你自己的知识库 id，写回 `config.json`（首次运行自动做）；
2. 批量往**已有**的库里灌内容（`import_urls`，一次 1–10 个链接）；
3. **完全绕开 IMA** —— 用你自己的离线快照，开箱即用。

### 给分享者（老余）的话

如果你想让对方拿到**完整能力（含产品库条款）**，需要你去 IMA 里做一次操作：
把四个库设成共享知识库、生成邀请链接，填进 `config.json → kb_share_links`。
步骤见 `docs/共享知识库配置.md`（约 5 分钟）。

**发布前请先想清楚**：产品库里的渠道佣金表、内部培训资料会对加入者可见。
不想公开的话，建议只共享【保来屋经纪】这一个库（老余观点，本来就是公开的），
其余走离线快照。

---

## 目录说明

```
.
├── 从这里开始.md                      ← 你在这
├── skills/                            ← 解压到 ~/.workbuddy/skills/
│   ├── laoyu-digital-human/
│   ├── yurenzhe-perspective/
│   ├── laoyu-client-grill/
│   └── baolaiwu/
├── ima-agent/
│   └── laoyu-ima-copilot-prompt.md    ← 想在 IMA 智能体里配老余时用（不走 WorkBuddy）
└── docs/
    ├── 依赖自检清单.md                 ← 老余体系全部技能的梳理与自检结果
    └── 共享知识库配置.md               ← 分享者操作：发布共享库、填链接
```

## 隐私说明

打包前已扫描，本包**不含**任何 API Key、Token、密码、身份证号：

- 四个技能目录 + 你导出的知识库快照，全量正则扫描密钥特征：0 命中；
- 手机号 `13407106946` 是老余的公开业务联系方式，按要求保留在技能中供引用；
- `wx_articles.json` 中命中的一串 18 位数字已核实为微信文章 URL 的 `sn` 参数，
  非身份证号，已排除。

如果你发现包里有任何应当移除的内容，告诉我，我重打。
